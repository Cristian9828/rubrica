# Database (sqlite | postgres)
DB_TYPE=sqlite
DB_PATH=./data/rubrica.db

# Postgres (only if DB_TYPE=postgres)
DB_HOST=postgres
DB_PORT=5432
DB_USER=rubrica
DB_PASS=rubrica
DB_NAME=rubrica

# JWT
JWT_SECRET=change-me-to-a-random-secret

# Server
PORT=3000
CORS_ORIGIN=http://localhost:5173
